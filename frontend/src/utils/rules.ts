/**
 * Числовые пределы правил Genesys, продублированные на клиенте только ради подсказок и
 * блокировки заведомо невозможных действий. Источник истины — backend domain (`GenesysRules`);
 * сервер всё равно проверяет каждый запрос и отклоняет невалидный.
 */

/** Максимальный ранг навыка до завершения создания, с учётом всех бесплатных источников. */
export const MAX_SKILL_RANK_AT_CREATION = 2

/** Сколько карьерных навыков можно отметить при создании для бесплатного ранга. */
export const MAX_FREE_CAREER_SKILLS = 4

/** Стандартное количество денег в кошельке при создании персонажа. */
export const STANDARD_STARTING_MONEY = 500
