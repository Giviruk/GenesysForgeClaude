# Исправления правил Core/RoT — прогресс реализации (`claude/rot-remediation-tasks`)

- **ТЗ:** [rot-rules-remediation-tasks.md](rot-rules-remediation-tasks.md) — источник истины по каждому пункту.
- **Ветка:** `feature/player-critical-rules` (предыдущая партия влита в master, PR #98)
- **Базовая ветка:** `master`
- **PR:** — (после создания)
- **Статус:** 🚧 In progress

## Контекст

ТЗ содержит ~95 задач (`ROT-*`) по 11 разделам: каждая требует правила в Domain/Application,
персистентности, безопасного API, UI и тестов. Каждая завершённая группа коммитится отдельно.

**Порядок работ изменён владельцем (27.07.2026).** Разделы 1–4 и ROT-CMB-01 закрыты по порядку
файла и влиты в master (PR #98). Дальше порядок — **не по файлу ТЗ, а по приоритету для игрока**:

1. **P1 — игрок создаёт персонажа по правилам, выбирает снаряжение и таланты.** Каталоги, которые
   игрок видит при выборе; числа, которые он видит на листе; покупка и возврат.
2. **P2 — инструменты ведущего.** Minion Groups, социальные столкновения, бестиарий, конструктор
   NPC, правила `ROT-CMB-04/05/06`, crafting и alchemy.

Пункты P2 остаются в чеклисте на своих местах и не считаются отменёнными — они просто идут после P1.

**Решение владельца по автоматизации.** Там, где эффект можно посчитать по-настоящему — расчёты
листа, производные характеристики, Game Table, проверки покупок — реализуется реальная механика.
Описанием ограничиваются только эффекты, которым нужны траты Story Point, счётчики применений в
scope сессии/encounter и единый check context: их нет в приложении, и изображать успешную
активацию нельзя. Метаданные (лимит, стоимость, триггер, тайминг) заполняются в обоих случаях.

Обязательные соглашения из §0 ТЗ (приоритет RoT → Core → FAQ/Errata v1.1, предварительная валидация
до первой мутации, стабильные `code`/ID вместо имён, versioned export, audit-запись, domain error с
машинным `reasonCode`) применяются ко всем пунктам и отдельно в чеклисте не повторяются.

## Чеклист по разделам ТЗ

### 1. Создание персонажа и стартовая прогрессия

- [x] **ROT-CRE-01** — видовые карьерные навыки и общий предел рангов
- [x] **ROT-CRE-02** — заморозка Wound/Strain Threshold после создания
- [x] **ROT-CRE-03** — стандартные деньги или целый карьерный комплект
- [x] **ROT-CRE-04** — комплект Scout с учётом errata

### 2. Видовые способности RoT

- [~] **ROT-SPECIES-01** — каталог, типизированные правила и пассивные эффекты сделаны;
  активируемые способности ждут состояния сессии (см. ниже)

### 3. Таланты RoT

- [x] **ROT-TAL-01** — состав каталога и metadata
- [x] **ROT-TAL-02** — prerequisites, взаимоисключения, покупка и refund
- [x] **ROT-TAL-03** — обязательные сохраняемые параметры талантов
- [~] **ROT-TAL-04** — данные выдач готовы и резолвер их учитывает; остался cost stack для refund
- [~] **ROT-TAL-05** — метаданные lifecycle описаны; рантайм активации не делаем по решению владельца
- [~] **ROT-TAL-06** — исправлены 24 записи с неверной механикой; остальные ждут боевого/магического движка
- [~] **ROT-TAL-07** — валидатор полноты написан и проходит на 112/112; ручной copyright-review не проводился

### 4. Heroic Abilities

- [x] **ROT-HA-01** — обязательные личное название и происхождение
- [~] **ROT-HA-02** — параметры Paragon, Sixth Sense и Signature Weapon сделаны; выбор transient
  base attachment ждёт движка attachments (ROT-EQP-ATT-02)
- [~] **ROT-HA-05** — матрица Power всех 11 эффектов и все 8 secondary effects выправлены по ТЗ;
  рантайм активации (session/encounter, счётчики, Story Points) не делается по решению владельца
- [~] **ROT-HA-08** — правило Miraculous Recovery выправлено; исполнение ждёт того же рантайма
- [~] **ROT-HA-10** — правило Unleash выправлено; исполнение ждёт Minion Groups и рантайма
- [x] **ROT-HA-CONTENT** — полные RU/EN парафразы, разделение PrivateFull и PublicSafe, валидатор

### 5. Бой, здоровье и Defense

- [x] **ROT-CMB-01** — обычный урон только при попадании, поглощение на каждый удар
- [x] **ROT-CMB-02** — преимущества ровно одной активной брони (P1)
- [x] **ROT-CMB-03** — Defense: provider/increase и предел 4 (P1)
- [ ] **ROT-CMB-04**, **ROT-CMB-05**, **ROT-CMB-06** (P2, инструменты ведущего)

### 6. Minion Groups

- [ ] **ROT-MIN-01** … **ROT-MIN-07**

### 7. Социальные столкновения

- [ ] **ROT-SOC-01** … **ROT-SOC-10**

### 8. Снаряжение, экономика, оружие, броня, attachments

- [~] **ROT-EQP-01** — перегруз и вес предметов с нулевым Enc сделаны; контейнеры/скакуны как
  отдельные holder'ы и правила сокрытия ждут своей персистентности (P1)
- [~] **ROT-ECO-01** — цена покупки, выручка от продажи и таблица редкости сделаны; priceless-товары
  и услуги как отдельные записи ждут схемы (P1), **ROT-EQP-02**, **ROT-EQP-GEAR-01**, **ROT-EQP-SVC-01**
- [ ] **ROT-MOUNT-ITEM-01**, **ROT-WPN-01**, **ROT-ARM-01**, **ROT-WPN-02**
- [ ] **ROT-EQP-ATT-01** … **ROT-EQP-ATT-03**, **GEN-EQP-DMG-01**, **ROT-EQP-AMMO-01**
- [ ] **GEN-EQP-QUAL-01**, **ROT-MAG-IMP-01**, **ROT-MAG-MAT-01**, **ROT-EQP-SRC-01**

### 9. Магия

- [ ] **ROT-MAG-01** … **ROT-MAG-08**, **ROT-MAG-10** … **ROT-MAG-12**, **ROT-MAG-CONTENT**

### 10. Magic items, crafting и alchemy

- [ ] **ROT-MITEM-01**, **ROT-CRAFT-01**, **ROT-CRAFT-MAGIC-01**
- [ ] **ROT-ALCH-01**, **ROT-ALCH-02**, **ROT-MITEM-CONTENT**

### 11. Очистка каталогов, бестиарий, скакуны

- [x] **ROT-CLEAN-3.1** — ровно девять карьер RoT, Knight исключён (P1)
- [x] **ROT-CLEAN-3.2** — Gunnery только в Genesys Core (P1)
- [x] **ROT-CLEAN-3.5** — одиннадцать лишних талантов не покупаются в RoT (P1)
- [ ] **ROT-CLEAN-3.6** — девять чужих противников в бестиарии (P2, инструмент ведущего)
- [x] **ROT-CLEAN-3.7** — удалён выдуманный Adventuring Pack (сделан вместе с ROT-CRE-03)
- [ ] **ROT-BEST-01**, **ROT-MOUNT-NPC-01**, **ROT-BEST-CONTENT**

### 11.1. Переработка конструктора NPC

- [ ] **ROT-NPC-01** … **ROT-NPC-13**

## Выполнено подробно

### ROT-CRE-01 — видовые карьерные навыки и общий предел рангов

- `ArchetypeStartingSkill.GrantsCareerSkill` + флаг в `archetypes.catalog.json` ровно у двух выдач:
  Deep Elf `Knowledge (Forbidden)`, Highborn Elf `Divine`.
- Новый доменный `CareerSkillResolver` (`Domain/Rules/CareerSkillResolver.cs`): карьера ∪ вид ∪
  таланты, дедупликация по `SkillDefId`, нерезолвленные имена возвращаются отдельно, а не теряются.
- `TalentDef.CareerSkillNames` заведён заранее — ветка талантов резолвера уже подключена, наполнение
  каталога делает ROT-TAL-04.
- Новый `CreationSkillPlan` (`Domain/Rules/CreationSkillPlan.cs`): создание собирает полный план всех
  бесплатных прибавок и проверяет предел ранга 2 **до** первой записи; превышение — ошибка с
  перечислением источников, обрезки ранга нет.
- Резолвер подключён в `CreateCharacterHandler`, `SheetBuilder` и `BuySkillRankHandler`; хранимый
  `CharacterSkill.IsCareer` остался кэшем.
- `CharacterSkillDto.CareerSources` + `ArchetypeStartingSkillDto.GrantsCareerSkill` в API;
  фронт показывает объединённый список карьерных навыков, источники в подсказке и disabled reason
  для уже добитого до 2 видового навыка.

### ROT-CRE-02 — заморозка порогов после создания

- `Character.CreationWoundThreshold/CreationStrainThreshold/ThresholdSnapshotProvenance/RulesReviewRequired`.
- `CompleteCreation` пишет snapshot в той же транзакции до смены фазы; повторный вызов идемпотентен.
- `SheetCalculator.ComputeDerived` принимает snapshot; явные бонусы порога прибавляются поверх один раз.
- Единый `CharacterDerived.Compute` — лист, список персонажей и Game Table больше не дублируют маппинг.
- Export поднят до `genesysforge.character.v2`; v1 читается с предупреждением и детерминированным
  расчётом вместо нуля. Duplicate копирует snapshot.
- Backfill в миграции восстанавливает характеристику на момент completion точно (текущая − выдачи
  Dedication), а не угадывает.

### ROT-CRE-03 / ROT-CRE-04 / ROT-CLEAN-3.7 — стартовое снаряжение

- `StartingEquipmentMode` (`StandardMoney` | `CareerPackage`), `Character.StartingPurchaseBudget`,
  `CharacterItem.Provenance`. Отсутствие поля режима у старого клиента = `StandardMoney`.
- Бюджет 500 и карманные 1d100 — **два разных счёта** (`StartingWallet`): покупка при создании
  тратит бюджет первым, продажа возвращает в бюджет первым, иначе цикл «купить → продать» превращал
  бы бюджет в реальные деньги.
- `MoneyFormula` разбирает `200 + 1d100` и бросает через инъецированный `IDiceRoller`
  (`SystemDiceRoller` на криптостойком RNG); формула и фактический бросок пишутся в audit
  (`CharacterAuditAction.CharacterCreated`).
- `CareerPackageResolver` выдаёт комплект только целиком: точное множество групп, ровно одна опция,
  без дублей и чужих опций; отказ — `DomainRuleException` с машинным `reasonCode`, который теперь
  доезжает до клиента в `ErrorResponse.ReasonCode`.
- Каталог `career-extras` переписан по таблице ТЗ для всех 9 карьер. Scout: первая группа — ровно
  `Bow` | `Light Spear`, `Leather Armor` вынесена в фиксированные, поэтому `Leather Armor ×2` больше
  не возникает. `Traveling Gear` разложен на 6 реальных предметов.
- Выдуманный `Adventuring Pack` помечен `Retired`; заодно `Retired` добавлен во все content-таблицы
  и включён в фильтры справочника — это база для раздела 11.
- Автоматическая раскладка legacy-паков выполняется только при доказанных provenance/quantity/state;
  иначе строка остаётся read-only.

### ROT-SPECIES-01 — виды (частично)

Сделано:

- Все 14 профилей сверены с таблицей ТЗ построчно (они уже были верны) и закреплены
  table-тестом на каждое значение, включая silhouette.
- `SpeciesAbilityRuleKind` + структурные параметры (`RuleValue`, `RuleParameters`, `UsesPerScope`,
  `UseScope`, `StoryPointCost`) на `ArchetypeAbilityDef`; все 19 способностей RoT типизированы.
  Механика больше не выводится из имени или описания.
- `ArchetypeDef.Silhouette` (гномы 0) и правило `Small` через тот же типизированный механизм.
- Nimble реализован как provider: `SheetCalculator` берёт max(броня, видовая база), поэтому
  Defense 1 с бронёй Defense 1 остаётся 1, а таланты по-прежнему прибавляются сверху.
- Обязательный выбор Half-Catfolk: `Character.SpeciesAbilityChoiceCode`, валидация при создании с
  `species.choice.required` / `unknown_option` / `not_applicable`, перенос в duplicate/export/import,
  `SpeciesChoiceIncomplete` на листе для legacy, пикер и гейт в форме создания.

Осталось (нужен рантайм сессии/encounter, которого в приложении ещё нет):

- Счётчики применений со scope session/encounter и их сброс в `EndSession`/`EndEncounter`.
- Активации, тратящие Story Point: Ready for Adventure, Tough as Nails, Tricksy.
- Правила, которым нужен единый check context с `sourceTag` и стабильной целью: Dark Vision,
  Stubborn, Battle Rage, Hot Tempered, Tenacious, Militia Training.
- Claws как virtual attack profile и структурированные manoeuvres для Fleet of Paw.

### ROT-TAL-01 — состав каталога талантов

- Манифест ТЗ разобран программно и сверен с каталогом: не хватало 3 талантов, 11 лишних были
  ошибочно доступны в RoT, 19 записей имели неверные tier/ranked/имя.
- Добавлены `Challenge!`, `Let’s Talk This Over`, `Retribution!` собственными парафразами.
- Одиннадцать талантов помечены `retiredIn: RealmsOfTerrinoth`: из RoT они исчезли, в Genesys Core
  остались активными, исторические владения не тронуты.
- Исправлены ranked (Apothecary, Blood Sacrifice, Body Guard, Dungeoneer, Exploit, Threaten),
  tier (Signature Spell 2, Signature Spell (Improved) 4, Conduit 4 unranked) и 11 канонических имён
  при прежних stable codes.
- `ActivationEn` + `CanUseOutOfTurn`: Out-of-turn Incidental стал отдельным таймингом;
  у Shapeshifter (Improved) — только по его триггеру.
- Сид перестал синхронизировать одни имена: tier, ranked, тайминг, out-of-turn, Retired и выдачи
  карьерных навыков теперь тоже приезжают из каталога.
- Данные ROT-TAL-04 (6 талантов, выдающих карьерные навыки) заполнены и уже учитываются
  резолвером из ROT-CRE-01.

### ROT-TAL-02 / ROT-TAL-03 / ROT-TAL-05

- `TalentPurchasePolicy`: retired, prerequisite и взаимоисключения проверяются до пирамиды, XP и
  любой мутации; отказ несёт машинный `reasonCode`. Возврат последнего ранга блокируется, если он
  остаётся основанием уже купленного зависимого таланта.
- Связи заполнены в каталоге по таблице ТЗ: 11 предусловий и 2 симметричные взаимоисключающие пары.
  `Inspiring Rhetoric (Supreme)` требует базовый, а не промежуточный Improved.
- `TalentChoiceSchema` + таблица `CharacterTalentChoices` с `rankIndex`, типизированным значением и
  снимком отображаемого имени. Валидация количества на ранг, различности между рангами и вида навыка
  выполняется до списания XP; возврат ранга снимает его выбор в той же транзакции.
- Legacy `GrantedCharacteristics` перенесены миграцией в общий формат; таланты, требующие выбора и не
  имеющие его, помечены `NeedsChoice` — выбирать за игрока и списывать XP повторно нельзя.
- ROT-TAL-05 сделан описательным по решению владельца: `UsesPerScope`, `UseScope`
  (session/encounter/round/turn), `StoryPointCost`, `StrainCost` и `Trigger` заполнены и показываются
  в UI бейджами, но счётчики и списания не выполняются.

### ROT-TAL-06 / ROT-TAL-07 — механика и тексты талантов

Исправлены 24 записи, где текст описывал неверное правило. Наиболее серьёзные:

- `Encouraging Song`, `Easy Prey`, `Back-to-Back` выдавали **чёрный кубик** вместо бонусного —
  то есть штраф вместо помощи. ТЗ отдельно предупреждает про эту подмену.
- `Conduit` был описан полностью неверно (раз за раунд снижал усталость на ранги); по правилу это
  раз за столкновение, одно очко сюжета, следующее магическое действие как манёвр.
- `Field Commander` содержал заглушку «Описание уточняется».
- `Signature Spell` обещал снижение сложности и отмену усталости; по правилу снижается только
  итоговая сложность на 1, усталость остаётся, предела применений нет, а Improved **заменяет**
  величину на 2, а не доводит до 3.
- `Berserk` был записан гарнитурными символами и терял 6 усталости на выходе и неприкосновенность
  дальнего боя.
- `Impaling Strike` не оговаривал, что доступен только после реально наложенного критического
  ранения и недоступен при дальней атаке.
- Уточнены `Precise Archery` (понижение ступени, а не снижение), `Rapid Archery` (Linked, попадание
  не выдаётся само), `Potent Concoctions` (кубик умения, отдельно на триумф и на отчаяние),
  `Shield Slam` и `Bullrush` (нет условия обязательного попадания), `Death Rage` (нет предела),
  `Battle Casting` и `Templar (Improved)` (оружие само по себе не условие).

`TalentContentValidator` (ROT-TAL-07) проверяет наличие содержательных RU и EN парафразов,
отсутствие заглушек и расхождение локалей; на текущем каталоге проходит 112/112.

### ROT-HA-01 — личное название и происхождение героической способности

- Три понятия разведены: primary effect каталога, `HeroicCustomName` (1–120) и origin. Название
  никогда не подменяется отображаемым именем эффекта — это видно на листе, в печати и в истории.
- `HeroicOriginMode` (`Standard` | `DoubleStandard` | `Custom`) хранится явно, поэтому потерянная
  вторая категория не выглядит как обычный одиночный результат.
- `HeroicOriginTable` — чистая доменная функция над инъецированным d10: специальный «0» даёт ровно
  два обычных результата, каждый повторный «0» перебрасывается, одинаковые обычные допускаются.
  Фактические грани сохраняются как напечатанные (`0,0,4,7`) ради аудита и финальным происхождением
  не становятся; сломанный RNG останавливается ошибкой, а не бесконечным циклом.
- Бросок выполняет сервер (`POST /heroic-identity/roll-origin`) и пишет `HeroicOriginRolled`;
  клиент не может прислать «выпавший» результат. Поля происхождения принимаются только вместе с
  явным режимом, иначе сохранённый бросок молча превратился бы в ручной выбор.
- `HeroicIdentityRules.Validate` проверяет запрос целиком до первой мутации; каждый отказ несёт
  машинный `reasonCode` (`name_required`, `origin_second_required`, `narrative_required` и т. д.).
- Completion невозможен без личности; после completion она неизменяема. Единственное исключение —
  однократное заполнение legacy-персонажа: до него улучшения героики заблокированы
  (`heroic.identity.incomplete`), после — запись снова закрывается.
- Duplicate, export v2 и импорт переносят личность; несовместимые поля в файле дают предупреждение
  и пустую личность вместо выдуманного происхождения.
- UI: выбрать категорию, описать своими словами или бросить; показываются обе категории
  специального результата и сами грани. Печать выводит личное название рядом с именем эффекта.

### ROT-HA-02 — параметры Paragon, Sixth Sense и Signature Weapon

- Требование параметра выводится из стабильного кода способности (`HeroicParameterRules.Required`),
  а не из отображаемого имени; кастомная способность параметра не требует.
- `CharacterHeroicConfiguration` — одна строка на персонажа: навык Paragon с снимком имени
  (скрытый позже кастомный навык даёт предупреждение о ремонте, а не молчаливую подмену) и
  типизированная категория Sixth Sense, а не общая заметка персонажа.
- `CharacterSignatureWeapon` — тоже одна строка, поэтому потерянное оружие и замена не могут быть
  активны одновременно. В базе хранится только выбор; skill/damage/crit/range/enc/HP/качества
  строит сервер из `SignatureWeaponProfiles` по таблице книги.
- `WeaponFormTraits` подтверждает GM, и совместимость улучшений будет считаться по ним, а не по
  тексту названия формы. Группу профиля всегда ставит сервер; невозможные сочетания (клинковое +
  дробящее, дальнобойный меч, одноручный лук) отклоняются, а меч сам получает Bladed и режущую кромку.
- Поля чужого параметра в запросе — ошибка `heroic.parameter.foreign_field`, а не игнорируемый мусор.
- Смена primary effect во время создания удаляет обе строки в той же транзакции. Completion требует
  параметр, после completion он неизменяем, у legacy-персонажа улучшения заблокированы до
  однократного выбора. Замена/возврат потерянного оружия — отдельная команда, доступная и после
  завершения создания.
- Export v2 переносит навык Paragon по коду и имени, а не по id: чужого id в другом аккаунте нет.

### ROT-HA-05 / 08 / 10 / CONTENT — тексты и режимы видимости героики

- Найдена и закрыта утечка режимов: полный текст способности отдавался как `SafeDescription`,
  то есть `PublicSafe` показывал полное правило, а `PrivateFull` не добавлял ничего. Теперь у
  каждой из 11 записей и каждого из 22 улучшений есть короткая safe-сводка и отдельный полный
  парафраз; улучшения тоже очищаются в `PublicSafe` (они не входят в content-model).
- Исправлены 18 текстов, расходившихся с матрицей ТЗ. Существенные:
  `All the Facts` (Supreme) — временное очко сюжета исчезает при трате и не переходит ведущему;
  `Unbowed` (Supreme) — смерть только отсрочена, при окончании способности персонаж умирает;
  `Unleash` (Base) — эффект стоит манёвра и не задевает соперников, немезид и PC;
  `Miraculous Recovery` — Improved не отменяет периодическое лечение, Supreme снимает выбранную
  травму без проверки; `Connected` — услуга ограничена и невозможная связь отменяет активацию без
  трат; `Foretelling` — одну проверку нельзя перебросить дважды, Supreme решает после обоих бросков.
- Восемь secondary effects переписаны целиком: до этого у них не было русского `PrivateFull`
  вовсе, а английский текст терял дальность, момент срабатывания и ограничения.
- `HeroicContentValidator` проверяет полноту RU/EN и то, что safe-сводка не совпадает с полным
  текстом; сид синхронизирует тексты существующих строк, а не только EN.

### ROT-CMB-01 — урон только при успешной атаке

- Расчёт урона жил только во фронтенде (`utils/combat.ts`) и при нуле успехов показывал базовый
  урон оружия, то есть рисовал урон на промахе; поглощение не применялось вовсе.
- Доменный `CombatResolver` — источник истины: попадание только при `netSuccesses > 0`,
  оставшийся триумф промах в попадание не превращает, `raw = base + successes`, а поглощение
  вычитается из **каждого** удара отдельно (Linked/Auto-fire не суммируются до вычитания).
- На промахе поля урона равны `null`, а не базовому урону. Активные качества на промахе
  отклоняются, кроме тех, чьё правило это прямо разрешает; трата, требующая прошедшего сквозь
  поглощение урона (обычное критическое ранение), отклоняется при полностью поглощённом ударе.
- `POST /api/combat/resolve-attack` отдаёт попадание, урон по каждому удару, допустимые и
  отклонённые траты и человекочитаемый лог. UI теперь пишет «Промах: обычный урон не применяется».
- Старый фронтовый тест закреплял ровно ошибочное поведение (`combatTotal(7, 0) === 7`) —
  переписан под правило.

### ROT-CLEAN-3.1 / 3.2 / 3.5 — каталоги, которые игрок видит при создании

- `Knight` был десятой карьерой RoT, которой в книге нет, а `Gunnery` сидился в набор навыков RoT.
  Обе записи игрок видел прямо в мастере создания.
- Убраны из сид-набора своей системы. Существующие строки не удаляются: новый шаг сида
  `RetireBuiltinsMissingFromSeed` помечает встроенную запись `Retired`, если её больше нет в наборе
  её системы. Сравнение идёт по натуральному ключу внутри системы, поэтому `Gunnery` остаётся
  активным навыком Genesys Core, а пользовательский контент не затрагивается вовсе.
- Гейты: retired-карьера не проходит и по присланному напрямую id
  (`career.not_available_in_system`), retired-навык не даёт купить новый ранг
  (`skill.not_available_in_system`), выбор стартовых навыков retired-записи не предлагает.
- Лист показывает retired-навык только тому персонажу, у которого по нему уже есть строка, и
  помечает его `SourceMismatch` — ранги не исчезают, но новый купить нельзя.
- ROT-CLEAN-3.5 был закрыт данными ещё в ROT-TAL-01, но политика покупки блокировала только
  **первый** ранг retired-таланта; повторный ранг проходил. Теперь отклоняются оба.

### ROT-CMB-02 — преимущества только одной активной брони

- Поглощение суммировалось по **всем** надетым предметам: две брони давали сумму двух поглощений.
  Это число игрок видел прямо на листе.
- `Character.ActiveArmorCharacterItemId` — ссылка на собственную надетую позицию. Защиту,
  поглощение и бонус порога веса даёт только она; прочая надетая броня продолжает считаться в
  переносимый вес. Не-броня (щит, талисман) обрабатывается отдельно и выбором брони не отключается.
- Первая надетая броня становится активной сама; надевание второй молча выбор не переключает.
  Снятие, удаление и полная продажа активной брони очищают выбор, следующая автоматически не
  выбирается. Чужая, ненадетая позиция или не-броня отклоняются с машинным `reasonCode`.
- Миграция выбирает legacy-броню детерминированно: максимальное поглощение, затем максимальная
  применимая защита, затем стабильный id. Предметы и их состояния не меняются.
- UI: радио-выбор в карточке брони и явная подпись, что неактивная надетая броня даёт только вес.

### ROT-CMB-03 — provider, increase и предел защиты

- `DefenseAggregator` разделил два принципиально разных вида источников: «получает Defense N»
  (броня, укрытие, видовая Nimble) конкурируют за максимум и между собой не складываются, а «+N»
  (Defensive, Deflection) складываются друг с другом и с лучшим провайдером. Итог ограничен 4.
- Раньше предела не было вовсе, а таланты прибавлялись к максимуму без различения режима.
- Вклады сверх предела не выбрасываются: после исчезновения лучшего провайдера пересчёт открывает
  следующий, а не обнуляет защиту.
- Каналы типизированы (`General` / `Melee` / `Ranged`), поэтому щит не защищает от стрел, а укрытие
  не помогает в ближнем бою.
- Разбор итога отдаётся на лист: победивший источник, проигнорированные провайдеры, надбавки и
  признак упора в предел — игрок видит подсказкой, почему число именно такое.
- Расчёт принимает внешние временные вклады (укрытие, стойка), поэтому Game Table сможет
  добавлять их, не трогая базовый snapshot.

### ROT-EQP-01 — перегруз и нулевой вес (частично)

- Перегруз был булевым флагом «перегружен», без цены. Теперь считается точное превышение: столько
  же костей помех к проверкам Мощи и Ловкости, а когда превышение достигает текущей Мощи —
  бесплатного манёвра нет и каждый манёвр, включая первый, стоит 2 усталости. Игрок видит это
  прямо в блоке переносимого веса.
- Предметы с нулевым Enc больше не бесплатны в любом количестве: они считаются вместе по всем
  позициям, каждые 10 россыпью дают 1 вес. Раньше сто факелов весили ноль, и десять отдельных
  строк обходили правило так же, как один стек.
- Осталось: типизированные holder'ы (контейнер, скакун, повозка), режим `Efficient` укладки
  (20 предметов на пункт) и правила сокрытия — им нужна отдельная персистентность placement.

### ROT-ECO-01 — деньги считает сервер (частично)

- Обе стороны сделки принимали сумму от клиента: `AddItemRequest.Cost` и `SellItemRequest.Proceeds`.
  Можно было купить латы за монету и продать кинжал за миллион — сервер верил запросу.
- Теперь стоимость покупки считается по цене каталога, а выручка — по результату проверки:
  провал не продаёт ничего, 1 успех — 25 %, 2 — 50 %, 3 и больше — 75 %. Доля берётся от цены
  **единицы** и округляется вниз, потом умножается на количество.
- Скидка за состояние не автоматическая: множитель задаёт ведущий явно и обязательно с причиной,
  как и цену вручную. И то, и другое целиком попадает в историю персонажа вместе с ценой каталога,
  долей и промежуточным итогом.
- Выдача без оплаты осталась, но стала явным флагом `Free`, а не «не передал цену».
- `TradeRules` содержит таблицу редкости и модификаторы рынка: редкость → сложность поиска,
  свыше 10 сложность не растёт, а каждый лишний пункт даёт ведущему одно улучшение проверки.
- UI больше не предлагает произвольный процент: при покупке видно цену × количество, при продаже —
  поле нетто-успехов и предварительный расчёт по тем же правилам.
- Осталось: `Price = —` как отдельный признак priceless (сейчас это просто цена 0) и услуги
  (ночлег, еда, дорога, найм) как записи расхода, а не вечные предметы инвентаря.

## Что осталось / блокеры

- Остаток ROT-TAL-06 — эффекты, которым нужен боевой, магический или социальный движок
  (`Shapeshifter` с автоматическим триггером формы, `Can’t We Talk About This?`, `Counterattack`,
  `Crushing Blow`). Сейчас они корректно описаны, но не исполняются.
- ROT-TAL-07 требует ручного copyright-review парафразов перед merge — он не проводился.
- Остаток ROT-HA-02 — выбор transient base attachment именного оружия: он использует те же
  predicates, что ROT-EQP-ATT-02, и делается вместе с движком attachments в разделе 8.
  Признаки формы под него уже хранятся.
- Исполняемая часть ROT-HA-05/08/10 (activation state, счётчики применений, периодические тики,
  Story Points, defeat minion group) требует рантайма сессии/encounter и Minion Groups — по решению
  владельца пока не делается. Тексты и метаданные под неё уже верны.
- ROT-HA-CONTENT: ручной copyright-review парафразов, как и для талантов, не проводился.
- Тесты по всем задачам пишутся в конце по решению владельца; уже написанные не удалялись.
- Незакрытая часть ROT-SPECIES-01 (активируемые способности) ждёт рантайма сессии/encounter.
- Незакрытая часть ROT-TAL-04 — cost stack фактически уплаченных цен за ранги и repair-флоу.
- ROT-TAL-04 требует cost stack покупок рангов (`CharacterSkillRankPurchase`) и repair-флоу для
  legacy без доказанной цены — крупнее остальных пунктов раздела 3, планировать отдельно.

## Заметки / решения

- `Assumption`: в §0.4 ТЗ разрешает типизированные domain-strategy вместо универсального
  JSON-интерпретатора — карьерные выдачи и план создания сделаны типизированными.
- Шаги 3–4 legacy-стратегии ROT-CRE-02 (восстановление из видимого итога и `LegacyEstimated`)
  в БД не нужны: шаг 2 применим всегда, потому что после создания характеристику меняет только
  Dedication и её выдачи сохранены. Оба провенанса всё же заведены — их использует импорт.
