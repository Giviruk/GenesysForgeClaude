# Исправить E2E-проверку формата экспорта

- **Пункт ТЗ:** исправление CI после обновления формата экспорта персонажа
- **Ветка:** `feature/fix-e2e-export-format`
- **Базовая ветка:** `master`
- **PR:** после создания
- **Статус:** 🚧 In progress

## Контекст

После удаления стартового бюджета экспорт персонажа был переведён на формат v8, но E2E smoke-тест
всё ещё ожидал v7. Из-за этого CI на `master` завершился с ошибкой только в E2E job.

## План выполнения

- [x] Обновить ожидание формата в E2E-тесте до v8.
- [x] Запустить frontend lint, build и 415 unit-тестов; локальный E2E невозможен без Docker daemon.
- [ ] Создать PR в `master` и проверить CI.

## Что осталось / блокеры

Локальный Docker daemon недоступен, поэтому E2E будет проверен GitHub Actions.

## Заметки / решения

Функциональный код экспорта не меняется; исправляется только устаревшее ожидание теста.
