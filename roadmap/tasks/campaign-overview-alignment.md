# Выравнивание обзора кампании (`feature/campaign-overview-alignment`)

- **Пункт ТЗ:** UI follow-up — восстановить выравнивание обзора кампании и центрировать только статистику группы (формальный ID в текущем коде не найден)
- **Ветка:** `feature/campaign-overview-alignment`
- **Базовая ветка:** `master`
- **PR:** [#245](https://github.com/Giviruk/GenesysForgeClaude/pull/245)
- **Статус:** 🚧 In progress — PR открыт, ожидает слияния

## Контекст

В предыдущей UI-доработке центрирование было применено ко всем блокам обзора кампании. Нужно вернуть для остальных блоков исходное выравнивание и сохранить центрирование только внутри блока «Статистика группы».

## План выполнения

- [x] Проверить текущую разметку и CSS обзора кампании
- [x] Ограничить центрирование классом блока «Статистика группы»
- [x] Восстановить исходное выравнивание остальных блоков обзора
- [x] Запустить frontend lint, Vitest и production build
- [ ] Copyright-проверка seed/справочников (не применимо: seed не меняется)
- [ ] Строка пункта в `rot-rules-remediation-progress.md` обновлена (не применимо: UI-задача вне списка правил)
- [x] PR открыт

## Что осталось / блокеры

После открытия PR дождаться CI. Backend, API, данные и бизнес-правила не меняются.

## Заметки / решения

- Assumption: блок «Статистика группы» — последний `.campaign-dash-block` внутри `.campaign-wide-grid` в `CampaignsPage.tsx`; ему добавлен отдельный класс `.campaign-party-stats`.
