# Читаемые ошибки завершения создания персонажа (character-creation-readable-errors)

- **Пункт ТЗ:** пользовательский запрос — понятные ошибки при завершении создания персонажа
- **Ветка:** `feature/character-creation-readable-errors`
- **Базовая ветка:** `master`
- **PR:** #не открыт
- **Статус:** ✅ Done locally; публикация PR заблокирована отсутствием `gh`/GitHub-доступа

## Контекст

Кнопка завершения создания персонажа отправляла запрос без обработки отказа. Ошибка могла попасть только в необработанное отклонение Promise, поэтому игрок не понимал, почему персонаж не завершён.

## План выполнения

- [ ] Найти ошибки завершения и определить человекочитаемые формулировки для способности, личности и параметра героической способности — готово локально.
- [ ] Добавить преобразование ошибок, обработку запроса завершения и защиту от повторного клика — готово локально.
- [ ] Добавить/обновить frontend-тесты — готово локально.
- [ ] Запустить lint, тесты и сборку frontend — успешно локально.
- [ ] PR открыт — `gh` недоступен в окружении.

## Что осталось / блокеры

Для совместимости с более старыми ответами UI также распознаёт ключевые фразы сообщения, если `reasonCode` отсутствует.

## Заметки / решения

- Меняем только frontend-поведение: игровые правила и серверные статусы не изменяются.
- Клиент сохраняет уже выдаваемый API машинный `reasonCode` в `ApiError`, чтобы UI ветвился по стабильному коду, а не по тексту.
- Проверки: `npm run lint`, `npm run build`, `npm test` — успешно; сборщик сообщил только существующее предупреждение о PURE-аннотациях в зависимости SignalR.
