# Выбор спутника для таланта «Животное-спутник» (animal-companion-choice)

- **Пункт ТЗ:** ROT-TAL-03 — обязательные сохраняемые параметры талантов
- **Ветка:** `feature/animal-companion-choice`
- **Базовая ветка:** `master`
- **PR:** [#172](https://github.com/Giviruk/GenesysForgeClaude/pull/172)
- **Статус:** ✅ PR #172 слит

## Контекст

Backend требует один непустой `AnimalCompanion` choice для каждого ранга таланта, но
`TalentsTab` открывает выбор только для Dedication. Нажатие «Купить» поэтому отправляет пустой
`choices` и получает `talent.choice.required` без возможности исправить запрос.

## План выполнения

- [x] Открывать форму спутника до покупки требующего её ранга
- [x] Передавать выбранного одобренного ведущим спутника в `choices`
- [x] Показывать сохранённый выбор на листе персонажа
- [x] Добавить frontend- и API-регрессионные тесты
- [x] Выполнить copyright-проверку (seed и тексты справочников не меняются)
- [x] Прогнать backend и frontend lint/tests/build
- [x] Открыть PR

## Что осталось / блокеры

Работа завершена, PR #172 слит. Блокеров нет.

## Заметки / решения

- Спутник выбирается из доступной игроку библиотеки NPC; сохраняется стабильный NPC id и снимок
  имени. Backend повторно проверяет видимость, систему и предел силуэта текущего ранга.
- `NpcListItem` аддитивно получил `silhouette`; схема БД не меняется, миграция не нужна.
- Проверки: backend 740 domain + 842 API; frontend 315 тестов, lint и production build.
- Модель данных и миграция не меняются: `CharacterTalentChoices` уже хранит выбор по рангу.
