# Testing Strategy

This file is kept as a compatibility note from the earlier documentation set.

Primary current testing documentation: [testing.md](testing.md).

Use [testing.md](testing.md) as the source of truth for:

- test projects found in the repository;
- commands;
- covered areas;
- critical gaps;
- future recommendations.

General rule: domain rules need domain tests; API shape and ownership need API tests; frontend helpers/API behavior need Vitest or React Testing Library tests.

